{
  "name": "tubelock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch && node scripts/copy-assets.js",
    "build": "vite build && node scripts/copy-assets.js",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "rebuild": "rm -rf dist && npm run build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
